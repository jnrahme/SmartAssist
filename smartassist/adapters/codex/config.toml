# SmartAssist MCP profile for Codex
# Copy into ~/.codex/config.toml or merge with existing config

project_doc_fallback_filenames = ["AGENTS.md", "CLAUDE.md"]

[mcp_servers.smartassist]
command = "smartassist"
args = ["serve"]
